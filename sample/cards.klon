Rank := List clone do(
	for(rank,2,10,add(rank asString))
	add("Jack") add("Queen") add("King") add("Ace")
)

Suit := list("Clubs", "Diamonds", "Hearts", "Spades")

Card := Object clone do(
	asString = method(rank + " of " + suit)
)

Deck := List clone do(
	init := method(
			Suit forEach(suit,
				Rank forEach(rank,
					card := Card clone
					card suit := suit
					card rank := rank
					add(card)
				)
			)
		)
	draw := method(pop)
)
